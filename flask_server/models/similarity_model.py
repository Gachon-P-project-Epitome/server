def compute_similarity(file_path, genre):
    # 여기에 유사도 계산 로직 구현
    # 예시 데이터
    similarity_score = 0.85  # 임의의 유사도 점수
    similar_tracks = [
        {'id': 'track_1', 'similarity': 0.9},
        {'id': 'track_2', 'similarity': 0.8},
        {'id': 'track_3', 'similarity': 0.75},
        {'id': 'track_4', 'similarity': 0.7},
        {'id': 'track_5', 'similarity': 0.65},
    ]
    return similarity_score, similar_tracks
